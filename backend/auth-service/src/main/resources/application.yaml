server:
  port: 8082

spring:
  application:
    name: auth-service

  config:
    import:
      - optional:file:E:/EKS-Deploy/eks-configs/application-${spring.profiles.active}.yml
      - optional:file:E:/EKS-Deploy/eks-configs/auth-service.yml
      - optional:file:E:/EKS-Deploy/eks-secrets/.env.${spring.profiles.active}[.properties]
      - optional:file:E:/EKS-Deploy/eks-secrets/auth-service/.env.${spring.profiles.active}[.properties]